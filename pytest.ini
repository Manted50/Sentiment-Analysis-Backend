[pytest]
minversion = 6.0
addopts = -v --tb=short
python_files = test_api.py
python_functions = test_*
markers =
    predict: Tests related to the /predict endpoint
    explain: Tests related to the /explain endpoint
    health: Tests related to the /health endpoint
